stages:
    - deploy
# Change pip's cache directory to be inside the project directory since we can
# only cache local items.
variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

# Pip's cache doesn't store the python packages
# https://pip.pypa.io/en/stable/reference/pip_install/#caching
#
# If you want to also cache the installed packages, you have to install
# them in a virtualenv and cache it as well.
cache:
  paths:
    - .cache/pip

dev.opensankey.fr:
  stage: deploy
  tags:
    - dev
  environment:
    name: dev
  only:
    - main
  cache: 
    key: $CI_COMMIT_REF_SLUG-$CI_PROJECT_DIR
    paths:
      - /home/ubuntu/dev_opensankey/opensankey/opensankey/client/node_modules/
    policy: pull
  before_script:
    - sudo systemctl stop dev_opensankey || true
  variables:
    NODE_ENV: "production"
  script:
    - cd /home/ubuntu/dev_opensankey
    - source dev_opensankey/bin/activate
    - cd opensankey
    - git pull
    - git checkout opensankey/server/views.py
    - cd opensankey/client
    - npm install
    - sudo npm link /home/ubuntu/dev_terriflux/su-model-sankey/sankeytools/client/node_modules/react
    - CI= npm run build
    - sed -i -e 's/\/static\//\/static\/opensankey\//g' ./build/index.html
    - CI= sudo npm run dist
    - sudo npm link .
    - mv node_modules ../../..
    - cd ../..
    - pip install .
    - source ../dev_terriflux/dev_terriflux/bin/activate
    - pip install .  
    - mv ../node_modules opensankey/client
    - cd dev_opensankey
    - sudo systemctl restart dev_opensankey


