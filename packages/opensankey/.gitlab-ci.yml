stages:
    - build
    - test
    - deploy
# Change pip's cache directory to be inside the project directory since we can
# only cache local items.
variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

# Pip's cache doesn't store the python packages
# https://pip.pypa.io/en/stable/reference/pip_install/#caching
#
# If you want to also cache the installed packages, you have to install
# them in a virtualenv and cache it as well.
cache:
  paths:
    - .cache/pip

# Change pip's cache directory to be inside the project directory since we can
# only cache local items.
variables:
  PIP_CACHE_DIR : "$CI_PROJECT_DIR/.cache/pip"
  MFAData       : "/home/ubuntu/dev_opensankey/MFAData"

build:
  stage: build
  tags:
    - dev, prod, test
  script:
    - source /home/gitlab-runner/gitlab/bin/activate
    - echo $CI_PROJECT_DIR
    - echo $EIGEN_INCLUDE
    - python setup.py bdist_wheel
  artifacts:
    paths:
      - dist/*.whl
  except:
    - schedules

test:
  stage: test
  tags:
    - dev, prod, test
  variables:
    GIT_STRATEGY: none
  script:
    - source /home/gitlab-runner/gitlab/bin/activate
    - pip install dist/OpenSankey-0.1-py3-none-any.whl
    - python -m unittest discover opensankey.tests
    - python -m unittest discover opensankey.tests
    - flake8 --exit-zero
  except:
    - schedules
  dependencies:
    - build

deploy_opensankey:
  stage: deploy
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        APP_NAME : "opensankey"
  tags:
    - dev
  environment:
    name: dev
    url: dev.opensankey.fr
  cache: 
    key: $CI_COMMIT_REF_SLUG-$CI_PROJECT_DIR
    paths:
      - $FOLDER/opensankey/opensankey/client/node_modules/
    policy: pull
  before_script:
    - sudo systemctl stop $APP_NAME || true
    - git config --global --add safe.directory /home/ubuntu/dev_opensankey/opensankey
  after_script:
    - echo "$FOLDER"
    - cd "$FOLDER"
    - bash restart_site.sh
  script:
    - echo "$FOLDER"
    - cd "$FOLDER"
    - source dev_opensankey/bin/activate
    - bash update_build_opensankey.sh
