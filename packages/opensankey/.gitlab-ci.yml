stages:
    - deploy
    - publish
# Change pip's cache directory to be inside the project directory since we can
# only cache local items.
variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

# Pip's cache doesn't store the python packages
# https://pip.pypa.io/en/stable/reference/pip_install/#caching
#
# If you want to also cache the installed packages, you have to install
# them in a virtualenv and cache it as well.
cache:
  paths:
    - .cache/pip


deploy_opensankey:
  stage: deploy
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    ENV   : dev
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        ENV: dev 
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        ENV: "test"
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        ENV: "prod" 
        APP_NAME : "opensankey"
  tags:
    - $ENV
  environment:
    name: $ENV
    url: dev.opensankey.fr
  cache: 
    key: $CI_COMMIT_REF_SLUG-$CI_PROJECT_DIR
    paths:
      - $FOLDER/opensankey/opensankey/client/node_modules/
    policy: pull
  before_script:
    - sudo systemctl stop $APP_NAME || true
  after_script:
    - echo "$FOLDER"
    - cd "$FOLDER"
    - bash restart_site.sh
  script:
    - echo "$FOLDER"
    - cd "$FOLDER"
    - source "$ENV"_opensankey/bin/activate
    - bash update_build_opensankey.sh

PublishExercicesAFM:
  stage: publish
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    ENV   : Exercices/AFM_dev
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        ENV: Exercices/AFM_dev 
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        ENV: Exercices/AFM_test 
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        ENV: Exercices/AFM_prod 
        APP_NAME : "prod_opensankey"
  tags:
    - $ENV
  environment:
    name: $ENV
    url: http://dev.open-sankey.fr/fm/userfiles/Exercices/AFM/artefacts/open-sankey_0.1.9.ExercicesAFM/index.html
  script:
    - cd $FOLDER/opensankey/opensankey/client
    - npm install
    - CI= npm run publish   --datapath="/home/ubuntu/MFAData/Exercices/AFM" --publish_name="ExercicesAFM"
    - CI= npm run build_zip --datapath="/home/ubuntu/MFAData/Exercices/AFM" --publish_name="ExercicesAFM"
    - CI= npm run unzip     --datapath="/home/ubuntu/MFAData/Exercices/AFM" --publish_name="ExercicesAFM"
  when: manual

PublishExercicesReconciliation:
  stage: publish
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    ENV   : Exercices/Reconciliation_dev
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        ENV: Exercices/Reconciliation_dev
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        ENV: Exercices/Reconciliation_test
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        ENV: Exercices/Reconciliation_prod
        APP_NAME : "prod_opensankey"
  tags:
    - $ENV
  environment:
    name: $ENV
    url: http://dev.open-sankey.fr/fm/userfiles/Exercices/Reconciliation/artefacts/open-sankey_0.1.9.ExercicesReconciliation/index.html
  script:
    - cd $FOLDER/su-model-sankey/sankeytools/client
    - npm install
    - CI= npm run publish   --datapath="/home/ubuntu/MFAData/Exercices/Reconciliation" --publish_name="ExercicesReconciliation"
    - CI= npm run build_zip --datapath="/home/ubuntu/MFAData/Exercices/Reconciliation" --publish_name="ExercicesReconciliation"
    - CI= npm run unzip     --datapath="/home/ubuntu/MFAData/Exercices/Reconciliation" --publish_name="ExercicesReconciliation"
  when: manual

PublishExercicesAgePoidsTaille:
  stage: publish
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    ENV   : Exercices/AgePoidsTaille_dev
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        ENV: Exercices/AgePoidsTaille_dev
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        ENV: Exercices/AgePoidsTaille_test
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        ENV: Exercices/AgePoidsTaille_prod
        APP_NAME : "prod_opensankey"
  tags:
    - $ENV
  environment:
    name: $ENV
    url: http://dev.open-sankey.fr/fm/userfiles/Exercices/AgePoidsTaille/artefacts/open-sankey_0.1.9.ExercicesAgePoidsTaille/index.html
  script:
    - cd $FOLDER/su-model-sankey/sankeytools/client
    - npm install
    - CI= npm run publish   --datapath="/home/ubuntu/MFAData/Exercices/AgePoidsTaille" --publish_name="ExercicesAgePoidsTaille"
    - CI= npm run build_zip --datapath="/home/ubuntu/MFAData/Exercices/AgePoidsTaille" --publish_name="ExercicesAgePoidsTaille"
    - CI= npm run unzip     --datapath="/home/ubuntu/MFAData/Exercices/AgePoidsTaille" --publish_name="ExercicesAgePoidsTaille"
  when: manual

PublishExercicesFormesCouleurs:
  stage: publish
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    ENV   : Exercices/FormesCouleurs_dev
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        ENV: Exercices/FormesCouleurs_dev
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        ENV: Exercices/FormesCouleurs_test
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        ENV: Exercices/FormesCouleurs_prod
        APP_NAME : "prod_opensankey"
  tags:
    - $ENV
  environment:
    name: $ENV
    url: http://dev.open-sankey.fr/fm/userfiles/Exercices/FormesCouleurs/artefacts/open-sankey_0.1.9.ExercicesFormesCouleurs/index.html
  script:
    - cd $FOLDER/su-model-sankey/sankeytools/client
    - npm install
    - CI= npm run publish   --datapath="/home/ubuntu/MFAData/Exercices/FormesCouleurs" --publish_name="ExercicesFormesCouleurs"
    - CI= npm run build_zip --datapath="/home/ubuntu/MFAData/Exercices/FormesCouleurs" --publish_name="ExercicesFormesCouleurs"
    - CI= npm run unzip     --datapath="/home/ubuntu/MFAData/Exercices/FormesCouleurs" --publish_name="ExercicesFormesCouleurs"
  when: manual

PublishFiliereVegetal:
  stage: publish
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    ENV   : Vegetal_dev
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        ENV: Vegetal_dev
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        ENV: Vegetal_test
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        ENV: Vegetal_prod
        APP_NAME : "prod_opensankey"
  tags:
    - $ENV
  environment:
    name: $ENV
    url: http://dev.open-sankey.fr/fm/userfiles/Végétal/artefacts/open-sankey_0.1.9.FiliereVegetal/index.html
  script:
    - cd $FOLDER/su-model-sankey/sankeytools/client
    - npm install
    - CI= npm run publish   --datapath="/home/ubuntu/MFAData/Végétal" --publish_name="FiliereVegetal"
    - CI= npm run build_zip --datapath="/home/ubuntu/MFAData/Végétal" --publish_name="FiliereVegetal"
    - CI= npm run unzip     --datapath="/home/ubuntu/MFAData/Végétal" --publish_name="FiliereVegetal"
  when: manual

PublishFiliereBoisSavoie1:
  stage: publish
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    ENV   : Bois/Savoie1_dev
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        ENV: Bois/Savoie1_dev
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        ENV: Bois/Savoie1_test
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        ENV: Bois/Savoie1_prod
        APP_NAME : "prod_opensankey"
  tags:
    - $ENV
  environment:
    name: $ENV
    url: http://dev.open-sankey.fr/fm/userfiles/Forêt Bois/Savoie/v1/artefacts/open-sankey_0.1.9.FiliereBoisSavoie1/index.html
  script:
    - cd $FOLDER/su-model-sankey/sankeytools/client
    - npm install
    - CI= npm run publish   --datapath="/home/ubuntu/MFAData/Forêt Bois/Savoie/v1" --publish_name="FiliereBoisSavoie1"
    - CI= npm run build_zip --datapath="/home/ubuntu/MFAData/Forêt Bois/Savoie/v1" --publish_name="FiliereBoisSavoie1"
    - CI= npm run unzip     --datapath="/home/ubuntu/MFAData/Forêt Bois/Savoie/v1" --publish_name="FiliereBoisSavoie1"
  when: manual

PublishFiliereBoisSavoie2:
  stage: publish
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    ENV   : BoisSavoie2_dev
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        ENV: Bois/Savoie2_dev
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        ENV: Bois/Savoie2_test
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        ENV: Bois/Savoie2_prod
        APP_NAME : "prod_opensankey"
  tags:
    - $ENV
  environment:
    name: $ENV
    url: http://dev.open-sankey.fr/fm/userfiles/Forêt Bois/Savoie/v1/artefacts/open-sankey_0.1.9.FiliereBoisSavoie2/index.html
  script:
    - cd $FOLDER/su-model-sankey/sankeytools/client
    - npm install
    - CI= npm run publish   --datapath="/home/ubuntu/MFAData/Forêt Bois/Savoie/v1" --publish_name="FiliereBoisSavoie2"
    - CI= npm run build_zip --datapath="/home/ubuntu/MFAData/Forêt Bois/Savoie/v1" --publish_name="FiliereBoisSavoie2"
    - CI= npm run unzip     --datapath="/home/ubuntu/MFAData/Forêt Bois/Savoie/v1" --publish_name="FiliereBoisSavoie2"
  when: manual

PublishFiliereBoisGrandEst:
  stage: publish
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    ENV   : BoisGrandEst_dev
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        ENV: Bois/GrandEst_dev
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        ENV: Bois/GrandEst_test
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        ENV: Bois/GrandEst_prod
        APP_NAME : "prod_opensankey"
  tags:
    - $ENV
  environment:
    name: $ENV
    url: http://dev.open-sankey.fr/fm/userfiles/Forêt Bois/Grand Est/artefacts/open-sankey_0.1.9.FiliereBoisGrandEst/index.html
  script:
    - cd $FOLDER/su-model-sankey/sankeytools/client
    - npm install
    - CI= npm run publish   --datapath="/home/ubuntu/MFAData/Forêt Bois/Grand Est" --publish_name="FiliereBoisGrandEst"
    - CI= npm run build_zip --datapath="/home/ubuntu/MFAData/Forêt Bois/Grand Est" --publish_name="FiliereBoisGrandEst"
    - CI= npm run unzip     --datapath="/home/ubuntu/MFAData/Forêt Bois/Grand Est" --publish_name="FiliereBoisGrandEst"
  when: manual

PublishFiliereBoisRegions:
  stage: publish
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    ENV   : BoisRegions_dev
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        ENV: Bois/Regions_dev
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        ENV: Bois/Regions_test
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        ENV: Bois/Regions_prod
        APP_NAME : "prod_opensankey"
  tags:
    - $ENV
  environment:
    name: $ENV
    url: http://dev.open-sankey.fr/fm/userfiles/Forêt Bois/France et Régions/artefacts/open-sankey_0.1.9.FiliereBoisRegions/index.html
  script:
    - cd $FOLDER/su-model-sankey/sankeytools/client
    - npm install
    - CI= npm run publish   --datapath="/home/ubuntu/MFAData/Forêt Bois/France et Régions" --publish_name="FiliereBoisRegions"
    - CI= npm run build_zip --datapath="/home/ubuntu/MFAData/Forêt Bois/France et Régions" --publish_name="FiliereBoisRegions"
    - CI= npm run unzip     --datapath="/home/ubuntu/MFAData/Forêt Bois/France et Régions" --publish_name="FiliereBoisRegions"
  when: manual

PublishFiliereLait:
  stage: publish
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    ENV   : Lait_dev
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        ENV: Lait_dev
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        ENV: Lait_test
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        ENV: Lait_prod
        APP_NAME : "prod_opensankey"
  tags:
    - $ENV
  environment:
    name: $ENV
    url: http://dev.open-sankey.fr/fm/userfiles/Lait/artefacts/open-sankey_0.1.9.FiliereLait/index.html
  script:
    - cd $FOLDER/su-model-sankey/sankeytools/client
    - npm install
    - CI= npm run publish   --datapath="/home/ubuntu/MFAData/Lait" --publish_name="FiliereLait"
    - CI= npm run build_zip --datapath="/home/ubuntu/MFAData/Lait" --publish_name="FiliereLait"
    - CI= npm run unzip     --datapath="/home/ubuntu/MFAData/Lait" --publish_name="FiliereLait"
  when: manual

PublishFiliereViande:
  stage: publish
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    ENV   : Viande_dev
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        ENV: Viande_dev
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        ENV: Viande_test
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        ENV: Viande_prod
        APP_NAME : "prod_opensankey"
  tags:
    - $ENV
  environment:
    name: $ENV
    url: http://dev.open-sankey.fr/fm/userfiles/Viande/artefacts/open-sankey_0.1.9.FiliereViande/index.html
  script:
    - cd $FOLDER/su-model-sankey/sankeytools/client
    - npm install
    - CI= npm run publish   --datapath="/home/ubuntu/MFAData/Viande" --publish_name="FiliereViande"
    - CI= npm run build_zip --datapath="/home/ubuntu/MFAData/Viande" --publish_name="FiliereViande"
    - CI= npm run unzip     --datapath="/home/ubuntu/MFAData/Viande" --publish_name="FiliereViande"
  when: manual

PublishFilierePaille:
  stage: publish
  variables:
    NODE_ENV: "production"
    FOLDER: /home/ubuntu/dev_opensankey
    ENV   : Paille_dev
    APP_NAME : dev_opensankey
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      variables:                              
        FOLDER: /home/ubuntu/dev_opensankey
        ENV: Paille_dev
        APP_NAME : dev_opensankey
    - if: $CI_COMMIT_REF_NAME == "test"
      variables:
        FOLDER: "/home/ubuntu/test_opensankey"
        ENV: Paille_test
        APP_NAME : "test_opensankey"
    - if: $CI_COMMIT_REF_NAME == "prod"
      variables:
        FOLDER: "/home/ubuntu/prod_opensankey"
        ENV: Paille_prod
        APP_NAME : "prod_opensankey"
  tags:
    - $ENV
  environment:
    name: $ENV
    url: http://dev.open-sankey.fr/fm/userfiles/Paille/artefacts/open-sankey_0.1.9.FilierePaille/index.html
  script:
    - cd $FOLDER/su-model-sankey/sankeytools/client
    - npm install
    - CI= npm run publish   --datapath="/home/ubuntu/MFAData/Paille" --publish_name="FilierePaille"
    - CI= npm run build_zip --datapath="/home/ubuntu/MFAData/Paille" --publish_name="FilierePaille"
    - CI= npm run unzip     --datapath="/home/ubuntu/MFAData/Paille" --publish_name="FilierePaille"
  when: manual