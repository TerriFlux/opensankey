# Migration VPS — dev.open-sankey.fr

**Ubuntu 20.04 → Ubuntu 24.04 LTS | Python 3.8 → 3.12 | Mai 2026**

> **Statut au 2026-05-16** : migration `dev` complétée et validée. L'app dev tourne
> sur le nouveau VPS, le runner GitLab CI dédié à dev (`vps-dev-py312-shell`,
> id `53197447`) est créé sur le nouveau VPS, tous les pipelines CI passent.
> Migrations `test` et `prod` à venir.

---

## 1. Contexte

Migration du VPS OVH de l'ancien serveur vers un nouveau VPS. Approche progressive : service par service, en commençant par l'environnement de développement.

| | Ancien VPS | Nouveau VPS |
|---|---|---|
| OS | Ubuntu 20.04 LTS | Ubuntu 24.04 LTS |
| Python | 3.8.10 | 3.12.3 |
| Node.js | — | 22.x (via NodeSource) |
| IP | ancienne IP | 51.75.204.192 |
| Port SSH | 22 (custom) | 5378 |

---

## 2. Étapes de migration

### 2.1 Préparation du serveur

**Changer le port SSH** — Ubuntu 24.04 utilise `ssh.socket`, pas `sshd_config` :

```bash
sudo systemctl edit ssh.socket
```

Ajouter dans l'éditeur :

```ini
[Socket]
ListenStream=
ListenStream=5378
```

```bash
sudo systemctl daemon-reload
sudo systemctl restart ssh.socket
```

Autoriser le port dans le pare-feu OVH (manager OVH) et UFW :

```bash
sudo ufw allow 5378/tcp
```

**Copier la clé SSH depuis le PC Windows** (ssh-copy-id n'existe pas sous Windows) :

```powershell
type ~/.ssh/id_ed25519.pub | ssh -p 5378 ubuntu@51.75.204.192 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

Se connecter ensuite avec :

```bash
ssh -p 5378 ubuntu@51.75.204.192
```

### 2.2 Installation de Node.js 22

`pnpm` (gestionnaire de paquets du projet) requiert Node.js >= v22.13. Ubuntu 24.04 apt ne fournit que Node 18.

```bash
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install nodejs -y
```

### 2.3 Clonage du dépôt et environnement virtuel

```bash
mkdir -p ~/dev_opensankey && cd ~/dev_opensankey
git clone git@gitlab.com:su-model/sankeyapplication.git
python3 -m venv dev_opensankey
source dev_opensankey/bin/activate
pip install uwsgi
```

Copier le fichier `env` depuis l'ancien serveur (variables d'environnement : clés Stripe, DB, etc.) :

```bash
# Depuis le PC, en passant par lui comme intermédiaire :
scp -P ANCIEN_PORT ubuntu@ANCIEN_IP:~/dev_opensankey/sankeyapplication/env ./env_dev
scp -P 5378 ./env_dev ubuntu@51.75.204.192:~/dev_opensankey/sankeyapplication/env
```

### 2.4 Lancement du script de déploiement

```bash
cd ~/dev_opensankey/sankeyapplication
bash update_opensankey.sh dev
```

### 2.5 Permissions du répertoire home

nginx (`www-data`) doit pouvoir traverser `/home/ubuntu` pour accéder au socket uWSGI :

```bash
chmod o+x /home/ubuntu
```

### 2.6 Initialisation de la base de données

Créer la structure de la base SQLite vide :

```bash
cd ~/dev_opensankey/sankeyapplication
source ~/dev_opensankey/dev_opensankey/bin/activate
alembic upgrade head
```

Puis copier la base depuis l'ancien serveur. Il faut d'abord autoriser la clé SSH du nouveau VPS sur l'ancien.

**Sur le nouveau VPS**, récupérer la clé publique :

```bash
cat ~/.ssh/id_ed25519.pub
```

**Sur l'ancien VPS**, ajouter la clé :

```bash
echo "COLLE_LA_CLÉ_ICI" >> ~/.ssh/authorized_keys
```

**Sur le nouveau VPS**, copier la base :

```bash
scp -P 5378 ubuntu@ANCIEN_IP:~/dev_opensankey/sankeyapplication/server/db.sqlite \
    ~/dev_opensankey/sankeyapplication/server/db.sqlite
```

> **Note :** un chemin de destination en `.\` créerait un fichier caché `.db.sqlite` — utiliser un chemin absolu.

Redémarrer le service :

```bash
sudo systemctl restart dev_opensankey
```

---

## 3. Corrections de compatibilité Python 3.12

Le passage de Python 3.8 à 3.12 a nécessité plusieurs corrections dans les submodules.

> **Statut au 2026-05-15** : tous les fixes de cette section sont désormais committés sur
> `su-model/mfa_problem@main` (commits `346b967` et `188907b`) et bumpés dans SA
> (`26c8f05f`). Les prochaines migrations (test, prod) n'ont **pas** à les ré-appliquer
> manuellement — un simple `git submodule update --recursive` suffit avant `update_opensankey.sh`.
> La section reste comme trace de ce qui a été fait.

### pnpm : ERR_PNPM_IGNORED_BUILDS

```bash
cd ~/dev_opensankey/sankeyapplication/client
pnpm approve-builds
```

(Non commité — interaction utilisateur côté machine cible.)

### flake8 (MFAProblem)

Deux problèmes en cascade :
1. `flake8 --exclude submodules` dans `MFAProblem/build_server.sh` écrasait l'exclusion
   du `setup.cfg`, faisant remonter des erreurs depuis `bin/archives`.
2. Après un build local sous Python 3.12, `build/lib.win-amd64-cpython-312/` contenait
   des sources flake8-positives non-trackées.

Fix appliqué dans `submodules/MFAProblem/setup.cfg` (centralise l'exclusion) :

```ini
[flake8]
max-line-length = 120
exclude = build, submodules, bin/archives, mfa_problem/tests
```

`build_server.sh` continue à passer `--exclude submodules,bin/archives` (l'env serveur ne
build pas en local et n'a pas de `build/`).

### scipy + numpy + cvxpy : versions compatibles Python 3.12

Pin maintenus stricts pour préserver la reproductibilité numérique des refs CI
(`TestData/ref_tests__*/`). Versions retenues dans `submodules/MFAProblem/setup.py` :

```python
"cvxpy==1.5.2",   # inchangé — wheel Py3.12 disponible
"osqp==1.0.5",    # inchangé — wheel Py3.12 disponible
"scipy==1.13.1",  # ex-1.10.1, dernière compat numpy 1.x + Py 3.10-3.13
"numpy==1.26.4",  # ex-1.24.4, dernière 1.x compat Py 3.9-3.12 (numpy 2.x serait breaking)
```

Validation : 71/71 tests MFAProblem passent en local sous Py3.12, refs JSON inchangées.

### setuptools : pkgutil.ImpImporter supprimé en Python 3.12

Sur la machine cible :

```bash
pip install --upgrade setuptools
```

Et dans `submodules/MFAProblem/build_server.sh`, pour éviter l'isolation de build (sinon
pip réinstalle un setuptools ancien) :

```bash
pip install --no-build-isolation .
```

### Build C++ local sous Windows (MSVC)

Ces 2 fixes ne sont nécessaires que pour builder MFAProblem localement sous Windows + MSVC
2022 — sans incidence sur les VPS Linux. Listés ici pour traçabilité :

- `mfa_problem_matrices.h` : ajout d'un `typedef SSIZE_T ssize_t` sous `_MSC_VER` (le type
  POSIX n'est pas exposé par MSVC).
- `tests/unit/test_mfa_problem_check_constraint.py` : 4 assertions sur
  `DataFrame.to_string()` réécrites pour comparer sur whitespace-tokenization (pandas 3.x
  a tightened le padding entre colonnes).

### pnpm v10 : ERR_PNPM_IGNORED_BUILDS en CI

pnpm v10 refuse par défaut d'exécuter les build scripts des dépendances non explicitement
approuvées (`@parcel/watcher`, `core-js`, `core-js-pure` chez nous). Sur les runners CI
vierges, `pnpm install` échoue avec `[ERR_PNPM_IGNORED_BUILDS]`.

Deux fixes appliqués (belt-and-suspenders) :

1. Dans chaque `client/package.json` (SA, OS+, OS), section explicative :
   ```json
   "pnpm": {
     "onlyBuiltDependencies": ["@parcel/watcher", "core-js", "core-js-pure"]
   }
   ```
2. Dans chaque `build_client.sh`, flag CLI sur `pnpm install` qui force le comportement
   même si la directive package.json est ignorée par certaines versions :
   ```bash
   pnpm install --config.dangerouslyAllowAllBuilds=true
   ```

(C'est la commande `--config.dangerouslyAllowAllBuilds=true` qui débloque effectivement —
la directive `package.json` seule s'est avérée silencieusement ignorée par le pnpm v10 du
runner.)

---

## 3bis. Migration du runner GitLab CI (imprévu de cette session)

Le runner CI tournait historiquement sur l'**ancien VPS** sous Python 3.8, avec un venv
partagé `/home/gitlab-runner/gitlab_dev`. Une fois le pin `scipy==1.13.1` poussé dans
MFAProblem (compatible Py3.12 uniquement), le runner ne pouvait plus installer le stack
en Py3.8 — toute la CI cassait.

> **Pourquoi pas deadsnakes PPA ?** Tenté : deadsnakes a abandonné le support de focal
> (Ubuntu 20.04, EOL avril 2025). Le `Packages.gz` du PPA pour focal est vide. Conda en
> userland marchait aussi mais ajoutait une couche.

**Solution retenue** : installer un nouveau runner GitLab sur le **nouveau VPS** (Ubuntu
24.04, Py3.12 natif) en parallèle de l'ancien. L'ancien reste actif pour servir
`test`/`prod` (tags `test`/`prod` sur jobs deploy lieu-spécifiques).

### Procédure runner sur le nouveau VPS

1. Côté GitLab UI (https://gitlab.com/groups/su-model → Build → Runners → New group
   runner) :
   - Tags : `py312`, **décocher "Run untagged jobs"**
   - Description : `vps-dev-py312-shell`
   - Récupérer le token `glrt-...` affiché (ne sera plus affiché ensuite).

2. Sur le nouveau VPS :
   ```bash
   curl -L "https://packages.gitlab.com/install/repositories/runner/gitlab-runner/script.deb.sh" | sudo bash
   sudo apt install gitlab-runner -y
   sudo gitlab-runner register   # interactif
   #   URL : https://gitlab.com/
   #   Token : (coller le glrt-...)
   #   Name : vps-dev-py312-shell
   #   Executor : shell
   ```

3. Créer le venv `gitlab_dev` en Py3.12 :
   ```bash
   sudo apt install python3-venv python3-dev -y
   sudo -u gitlab-runner python3 -m venv /home/gitlab-runner/gitlab_dev
   sudo -u gitlab-runner /home/gitlab-runner/gitlab_dev/bin/pip install --upgrade pip setuptools wheel
   sudo -u gitlab-runner /home/gitlab-runner/gitlab_dev/bin/pip install uwsgi pytest
   ```
   (`pytest` parce que les jobs `test` en ont besoin et requirements.txt des submodules
   ne le listent pas.)

4. Côté `.gitlab-ci.yml` de chaque projet : tagger `py312` les jobs qui activent
   `gitlab_dev`. Voir les commits CI de mai 2026 sur SEP, OS, OS+, SA. Les jobs
   `test_opensankey` et `prod_opensankey` (SA) gardent leurs tags `test`/`prod`.

5. À chaque modification SEP qui demande une réinstall : si tu ajoutes une `install_requires`
   manquante (comme `orjson`/`webcolors`/`packaging` qu'on a ajouté à SEP@3955034),
   c'est consommé directement par la cascade pip — pas de touch runner nécessaire.

### Décommissionnement futur de l'ancien runner

Quand `test` et `prod` seront migrés sur le nouveau VPS, l'ancien runner pourra être
arrêté/déregistered. À ce moment-là, tagger `py312` les jobs `test_opensankey` et
`prod_opensankey` aussi.

---

## 4. Service systemd

Créer `/etc/systemd/system/dev_opensankey.service` :

```ini
[Unit]
Description=uWSGI instance to serve developpement version of sankeyapp
After=network.target

[Service]
User=ubuntu
Group=www-data
WorkingDirectory=/home/ubuntu/dev_opensankey/sankeyapplication
EnvironmentFile=/home/ubuntu/dev_opensankey/sankeyapplication/env
ExecStart=/home/ubuntu/dev_opensankey/dev_opensankey/bin/uwsgi --ini sankeyapp.ini

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
```

### restart_site.sh

Le script `restart_site.sh` appelle `systemctl restart/start/enable` — le service doit exister avant le premier `update_opensankey.sh`.

---

## 5. Nginx et SSL

Créer `/etc/nginx/sites-available/dev_opensankey` :

```nginx
server {
    server_name dev.open-sankey.fr www.dev.open-sankey.fr;

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/home/ubuntu/dev_opensankey/sankeyapplication/sankeyapp.sock;
        try_files $uri /index.html;
        if ($request_method ~* "(GET|POST)") {
            add_header "Access-Control-Allow-Origin" *;
        }
    }

    location /stripe/webhook {
        include uwsgi_params;
        uwsgi_pass unix:/home/ubuntu/dev_opensankey/sankeyapplication/sankeyapp.sock;
        try_files $uri /index.html;
        if ($request_method ~* "(GET|POST)") {
            add_header "Access-Control-Allow-Origin" *;
        }
    }

    listen 80;
}
```

Activer et recharger :

```bash
sudo ln -s /etc/nginx/sites-available/dev_opensankey /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
```

Mettre à jour le DNS dans OVH (Zone DNS) : enregistrement A de `dev.open-sankey.fr` → `51.75.204.192`, vérifier la propagation :

```bash
dig dev.open-sankey.fr +short
```

Obtenir le certificat SSL :

```bash
sudo certbot --nginx -d dev.open-sankey.fr
```

> **Note :** ne pas inclure `www.dev.open-sankey.fr` si l'enregistrement DNS www n'existe pas.

---

## 6. Pour les prochaines migrations (test / prod)

Tous les fixes Python 3.12 sont désormais committés upstream — il n'y a plus de fix
manuel à appliquer aux submodules sur la machine cible. Procédure réduite :

1. Préparer le VPS cible (§2.1 SSH, §2.2 Node 22, §2.5 permissions home).
2. Créer le virtualenv et cloner le repo (adapter le chemin : `test_opensankey`,
   `prod_opensankey`).
3. Copier le fichier `env` (§2.3).
4. `bash update_opensankey.sh [test|prod]` — devrait dérouler.
5. Créer le service systemd (`test_opensankey.service`, `prod_opensankey.service`),
   adapter `WorkingDirectory` et `EnvironmentFile`.
6. Créer la config nginx et activer le site.
7. Mettre à jour le DNS et lancer certbot.
8. (Optionnel) Initialiser/copier la base SQLite depuis l'ancien serveur (§2.6).

### Piège : modifs locales dans submodules/MFAProblem qui empêchent le git pull

Si l'ancien VPS dev a eu des fixes manuels appliqués pendant la phase initiale (avant
qu'ils soient committés upstream), le `git submodule update --recursive` après `git pull`
échoue avec :

```
error: Your local changes to the following files would be overwritten by checkout:
        build_server.sh
        setup.cfg
        setup.py
```

Solution (modifs locales obsolètes maintenant que tout est upstream) :

```bash
cd submodules/MFAProblem
git checkout -- build_server.sh setup.cfg setup.py
cd ../..
git submodule update --recursive
```

Pour les migrations `test`/`prod` neuves (qui n'auront jamais eu de fix manuel), ce piège
ne se présentera pas.

### Côté GitLab CI

L'ancien runner sur l'ancien VPS continue à servir les jobs `test_opensankey` et
`prod_opensankey` (tags `test`/`prod`). Aucun changement nécessaire tant que test et
prod ne sont pas migrés. Après leur migration, voir §3bis "Décommissionnement".
