alembic
flask==2.3.3
flask_cors
flask_login
flask-mail
flask-SQLAlchemy==2.5.1
sphinx
sphinx_rtd_theme
sphinx-multibuild
SQLAlchemy==1.4.25
werkzeug==2.3.7
itsdangerous
stripe
sentry-sdk[flask]
anthropic>=0.69.0
openpyxl
markdown
beautifulsoup4
# Miniatures de portfolio générées au publish (server/publish_thumbnails.py) :
# rendu headless du diagramme. Le binaire Chromium s'installe à part sur l'infra
# (`playwright install chromium`) ; sans lui, le publish dégrade proprement
# (cartes sans image), donc l'absence du navigateur ne casse rien.
playwright>=1.40
flake8>=7.1.0
pycodestyle>=2.12.0